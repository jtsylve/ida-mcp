# ida-mcp

This is a backwards-compatible alias package that installs [`re-mcp-ida`](https://pypi.org/project/re-mcp-ida/). Use either `pip install ida-mcp` or `pip install re-mcp-ida` — both install the same IDA Pro backend for [RE-MCP](https://github.com/jtsylve/ida-mcp).
